<Page x:Class="SampleClient.RoomList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SampleClient"
      xmlns:vm="clr-namespace:SampleClient.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RoomList">

    <Page.DataContext>
        <vm:RoomListViewModel />
    </Page.DataContext>

    <Grid>
        <ListView ItemsSource="{Binding Rooms}" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Room Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                    <GridViewColumn Header="Capacity" DisplayMemberBinding="{Binding Capacity}" Width="70"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
